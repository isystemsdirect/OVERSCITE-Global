# OVERSCITE™ Deployment Guide

## Simulation Deployment
The primary deployment mode for v0.1.2. All SCIMEGA™, telemetry, and autonomy systems operate in high-fidelity simulation. No external hardware dependencies are required.

## Dry-Link Deployment
Optional mode enabling metadata-only synchronization with physical TelePort nodes. Requires network configuration for telemetry intake. No command-send capability is enabled.

## No-C2 Production Disclaimer
**This system does not provide Command & Control (C2) capability in any deployment configuration.** All drone orchestration is limited to simulation and dry-link modes. No live hardware mutation pathways exist.

## Current Posture
**SIMULATION / DRY-LINK / NO LIVE HARDWARE**
